<body>

  <div class="row row-cols-1 row-cols-md-3">
   {{#each pokemon as |poke|}}
  <div class="col mb-4">
  <div class="card">
    <img src="{{image}}" class="card-img-top" alt="...">
    <div class="card-body">
      <h5 class="card-title">{{name}}</h5>
      <p class="card-text">HP: {{hp}}  Attack: {{attack}}<br>
      Move1: {{move1}} Move:2 {{move2}}</p>
     
    </div>
  </div>
  </div>
  {{/each}}
</div>
